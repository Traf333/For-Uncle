.row-fluid
  .span5
    = link_to seller.name, seller
  .span5
    - if current_seller != seller && current_seller.admin?
      = link_to "Удалить", seller, method: :delete, data: { confirm: "Вы уверены?" }

%br
